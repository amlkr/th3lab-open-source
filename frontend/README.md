# th3lab Frontend (Vite)

## Development

```bash
npm install
npm run dev
```

## Production Build

```bash
npm run build
```

## API Base URL

The app reads `VITE_API_BASE_URL` from environment variables.

- Default (if unset): `http://localhost:8000`
- Example: copy `.env.example` to `.env` and set your value
